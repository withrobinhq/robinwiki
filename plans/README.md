# Implementation Plans

Generated by the improve skill on 2026-06-11 against commit `21533c8`.
Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update
your row when done.

Selection note: session ran non-interactively, so the top 5 findings by
leverage were planned by default (the skill's documented fallback). The
findings table and rejected list below record everything else.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Restore verification baseline (typecheck + all tests green) | P1 | M | — | TODO |
| 002 | CI gate: typecheck + full test suite on every PR | P1 | S | 001 | TODO |
| 003 | Dependency security sweep (next, hono, better-auth, vitest, transitive) | P1 | M | 001 | TODO |
| 004 | CSRF Origin check on session-authenticated mutations | P1 | S | 001 | TODO |
| 005 | Transactional wiki delete + conditional classify inserts (zombie edges) | P2 | M | 001 | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 001 first, always: typecheck and tests fail on clean main today, so no other
  plan's verification gates work until it lands.
- 002 immediately after 001, or the suite rots again.
- 003/004/005 are independent of each other; 004 and 005 both touch core but
  disjoint files (middleware/session.ts vs routes/wikis.ts + lib/regen.ts).
  005 also edits `core/src/routes/wikis.ts` — rebase its drift check if 004's
  branch lands first (004 doesn't touch wikis.ts, so no conflict expected).

## Unplanned findings (real, deferred — promote to plans on request)

- **classifyUnfiledFragments starvation** (`core/src/lib/regen.ts:280-292`):
  loads ALL filed fragment keys into memory, then post-filters a 100-row
  hybrid-search result down to 50 — fragments ranking below the top 100 can
  never be swept into a wiki by this path. Push the unfiled filter into SQL
  (pattern exists at `wikis.ts:264-267`). S effort, MED impact.
- **CLAUDE.md is stale**: claims "Technology stack not yet documented /
  Architecture not yet mapped" while `docs/architecture/` holds 7 detailed
  docs and README documents the stack. S effort; misleads every agent session.
- **`sed -i` hack in wiki manifest script** (`wiki/package.json` `manifest`):
  GNU-sed-only, silently no-ops on macOS BSD sed. Replace with a small Node
  script or fix `@hey-api/openapi-ts` output config. S effort.
- **`@hey-api/client-fetch` deprecated upstream** (wiki's generated API client
  transport). M effort migration to the maintained equivalent.

## Findings considered and rejected (do not re-audit)

- **better-auth "version skew" (core ^1.0.0 vs wiki ^1.6.2)**: lockfile
  resolves both to 1.6.2 — no runtime skew. Range alignment folded into plan 003.
- **Missing index on `wikis.updatedAt` for list sort**: single-user deployment,
  hundreds of rows — a seq scan is sub-millisecond. Not worth schema churn.
- **N+1 edge-insert loops** (`wikis.ts:275-288`, `regen.ts:181-237`): bounded
  at 20 rows per call by the candidate `.limit(20)`. Negligible.
- **`WikiEntityArticle.tsx` code-splitting**: speculative without profiling
  data; component tests exist and the page renders fine.
- **MCP resolvers lack userId filtering (IDOR)**: by design — single-user
  collapse (documented in `middleware/session.ts`). Revisit only if multi-user
  ever lands.
- **X-Forwarded-Host trusted when deriving publishedOrigin**
  (`wikis.ts:707-726`): deliberate (comment explains multi-host deploys),
  endpoint is session-authenticated, and DEPLOY.md documents the
  trusted-proxy requirement. Optional hardening: validate against the
  WIKI_ORIGIN allowlist (helper created by plan 004 makes this trivial).
- **Per-process lockout on keypair reveal**: single-instance deployment model;
  latent multi-instance concern only.
- **TypeScript version skew (core 5.x, wiki 6.0.2)**: each workspace
  typechecks with its own compiler; wiki is intentionally independent
  (CLAUDE.md constraint).
- **Audit double-emission condition** (`regen.ts:214`, `||` vs `&&`): the OR
  is arguably correct crash-recovery semantics (partial insert → events were
  never emitted); audit-log-only impact either way.
- **Rate limiting on public `/published` reads**: optional hardening for a
  low-traffic single-user surface; revisit if the public reader is productized.

## Direction findings (strategic options, not defects)

- **Productize the public wiki reader**: 8+ recent `feat(published)` PRs show
  it nearly done; remaining gaps are operator docs and publish-flow polish.
- **Surface authorship/provenance in the UI**: schema landed in `a39cf35`
  (structured authorship for entries/fragments); the UI layer is the cheap
  next step.
- **Decide the ZDR-embeddings question deliberately**: spec was added
  (`6fb5e32`) and reverted (`3c1f152`) with no recorded rationale.
- **Multi-org/enterprise**: an org_id schema change was added and reverted in
  git history — a strategic fork worth an explicit written decision either way.
